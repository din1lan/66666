// Superseded by src/modules/cases/CasesPage.jsx, which merged the deadline
// calendar/list UI with the case table + lawyer-folder filtering. Kept as a
// re-export (rather than deleted) because this sandbox's mounted output
// directory doesn't allow deleting files once written — not imported from
// anywhere in the app.
export { default } from '../cases/CasesPage.jsx'
